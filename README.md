# Render

Automate [https://dashboard.render.com/login](https://dashboard.render.com/login) with Claude using this Conxa plugin.

## Install

```
npx -y conxa install render
```

This installs the plugin into the shared Conxa runtime (`~/.conxa/`). One MCP
server (`conxa`) serves every installed plugin — no per-plugin MCP setup.

## Available Skills

- `delete-a-service-66f54268` — see `skills/delete-a-service-66f54268/SKILL.md`

## How It Works

This plugin works with the shared `conxa` MCP server. When Claude calls a skill,
a real Chromium browser opens on your machine, executes the recorded workflow,
and returns a result and screenshot. Your auth session stays on your machine
and is never uploaded anywhere.

---
*Generated by [Conxa](https://conxa.ai)*
